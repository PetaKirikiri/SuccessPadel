/** Shared schedule numbers — keep this file free of imports to avoid init-order cycles. */

export const RANKED_AMERICANO_GAMES = 6
export const RANKED_GAME_MINUTES = 15
/** Default competition window when ends_at is not set yet (e.g. draft league week). */
export const DEFAULT_COMPETITION_EVENT_MINUTES = 110
